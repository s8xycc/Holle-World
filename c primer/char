#include <stdio.h>
int main(void)
{
    char ch, n, x, y;
    n = '\\';
    x = 'h';
    y = 64;
    
    printf("%c; %c; %c", n, x, y);
    
    printf("Please enter a character.");
    
    scanf_s("%c", &ch);
    
    printf("The code for %c is %d", ch, ch);
    
    return 0;
}
